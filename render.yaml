services:
  # Servicio Web: Frontend (React + Vite, servido con Nginx)
  - type: web
    name: tutu-frontend
    env: docker
    rootDir: frontend
    dockerfilePath: ./Dockerfile
    # Render free tier
    plan: free
    envVars:
      - key: VITE_API_BASE_URL
        value: # TODO: Pega aquí la URL pública de tu backend de Render cuando esté desplegado
